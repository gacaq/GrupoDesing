<h1>Roles</h1>

<table class="table span8 striped"> 
  
  <thead>
    <tr>
    <th>Rol</th>
    <th>Descripción</th>
    </tr> 
  </thead>

  <tbody>
    <% @roles.each do |role| %>
      <tr>
        <td><%= role.role %></td>
        <td><%= role.description %></td>
        <td><%= link_to 'Ver', role, class: "btn btn-info" %></td>
        <td><%= link_to 'Editar', edit_role_path(role), class: "btn btn-success" %></td>
        <td><%= link_to 'Eliminar', role, class: "btn btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

